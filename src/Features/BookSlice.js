import { createSlice } from '@reduxjs/toolkit'

export const TotalBookSlice = createSlice({
  name: 'book',
  initialState: {
  },
  reducers: {
    
  }
})


export const {} = TotalBookSlice.actions

export default TotalBookSlice.reducer